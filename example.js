// import { createStore } from "redux"
import { createStore } from "./fakeRedux";

`
  createStore(reducer) 
  返回 store = {
    getState: func,
    subscribe: func,
    dispatch: func,
  }
`
`
Redux 最简单用例
store 中存储一个 count 用于计数，初始值为 0

store = {
    count: 0
}

通过 addOneCount 让 store 中 count 的值加一
`;
// action
// const addOneCountString

// action creator
// const addOneCount


// reducer
// function counter() {
// }




// store
// let store = createStore();


// store.subscribe

// store.dispatch
// store.dispatch
